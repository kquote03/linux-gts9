# Enablement policy for this port's units.  systemd-preset-all runs on first
# boot and strips enablement from anything without a preset entry, which is
# how the first Fedora boot silently lost the whole device stack.
#
# Sensor discovery starts from a timer, outside the graphical boot chain,
# and pulls in sensorspd/ADSP after panel recovery. Those two services are
# not independently enabled: ADSP startup concurrent with the panel's PM
# test previously froze this board. gts9wifi-resume restarts discovery after
# sleep; neither the timer nor discovery is allowed to run during sleep.
# Removed reference-only units (mem-reclaim, vendor.mount, android-parts)
# remain excluded; see docs/porting-log.md.
enable sshd.service
enable NetworkManager.service
enable hexagonrpcd-adsp-rootpd.service
enable pd-mapper.service
enable gts9wifi-bt-provision.service
disable gts9wifi-wait-sensor-proxy.service
enable gts9wifi-wait-sensor-proxy.timer
enable gts9wifi-resume.service
enable gts9wifi-panel-coldboot-recover.service
enable gts9wifi-grow-rootfs.service
enable gts9wifi-usb-net.service
enable gts9wifi-wifi-recover.service
enable gts9wifi-sensor-registry-perms.service
enable gts9wifi-x11-dir-fix.timer
disable gts9wifi-x11-dir-fix.path
enable gts9wifi-chronyd.service
enable gts9wifi-audio-init.service
enable mnt-vendor-persist.mount
enable vendor-dsp.mount
enable vendor-firmware_mnt.mount
